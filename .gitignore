node_modules/
test-results/
playwright-report/
qa/report/
.last-run.json
.autofix-state.json
*.bak
.DS_Store

# Machine-local Claude Code permissions (home paths, per-machine allowlist)
.claude/settings.local.json

# Browser-session artifacts from MCP runs
.playwright-mcp/

# Client QA work for third-party sites — this repo is public, keep it out
.qa/lavinmedia/

# Visual baselines: only Docker-generated -linux renders are committed.
# Local platform baselines are kept for fast pre-push feedback but never
# shared - one render environment, one set of files, one truth to review.
tests/**/*-darwin.png
tests/**/*-win32.png

# Visual baselines: only Docker-generated -linux renders are committed.
# Local platform baselines are kept for fast pre-push feedback but never
# shared - one render environment, one set of files, one truth to review.
tests/**/*-darwin.png
tests/**/*-win32.png
